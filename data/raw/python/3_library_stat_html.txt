stat â Interpreting stat() resultsÂ¶
Source code: Lib/stat.py
The stat module defines constants and functions for interpreting the
results of os.stat(), os.fstat() and os.lstat() (if they
exist).  For complete details about the stat(), fstat() and
lstat() calls, consult the documentation for your system.
Changed in version 3.4: The stat module is backed by a C implementation.
The stat module defines the following functions to test for specific file
types:
- stat.S_ISDIR(mode)Â¶
- Return non-zero if the mode is from a directory.
- stat.S_ISCHR(mode)Â¶
- Return non-zero if the mode is from a character special device file.
- stat.S_ISBLK(mode)Â¶
- Return non-zero if the mode is from a block special device file.
- stat.S_ISREG(mode)Â¶
- Return non-zero if the mode is from a regular file.
- stat.S_ISFIFO(mode)Â¶
- Return non-zero if the mode is from a FIFO (named pipe).
- stat.S_ISLNK(mode)Â¶
- Return non-zero if the mode is from a symbolic link.
- stat.S_ISSOCK(mode)Â¶
- Return non-zero if the mode is from a socket.
- stat.S_ISDOOR(mode)Â¶
- Return non-zero if the mode is from a door. Added in version 3.4.
- stat.S_ISPORT(mode)Â¶
- Return non-zero if the mode is from an event port. Added in version 3.4.
- stat.S_ISWHT(mode)Â¶
- Return non-zero if the mode is from a whiteout. Added in version 3.4.
Two additional functions are defined for more general manipulation of the fileâs mode:
- stat.S_IMODE(mode)Â¶
- Return the portion of the fileâs mode that can be set by os.chmod() âthat is, the fileâs permission bits, plus the sticky
bit, set-group-id, and set-user-id bits (on systems that support them).
- stat.S_IFMT(mode)Â¶
- Return the portion of the fileâs mode that describes the file type (used by the S_IS*() functions above).
Normally, you would use the os.path.is*() functions for testing the type
of a file; the functions here are useful when you are doing multiple tests of
the same file and wish to avoid the overhead of the stat() system call
for each test.  These are also useful when checking for information about a file
that isnât handled by os.path, like the tests for block and character
devices.
Example:
import os, sys
from stat import *
def walktree(top, callback):
    '''recursively descend the directory tree rooted at top,
       calling the callback function for each regular file'''
    for f in os.listdir(top):
        pathname = os.path.join(top, f)
        mode = os.lstat(pathname).st_mode
        if S_ISDIR(mode):
            # It's a directory, recurse into it
            walktree(pathname, callback)
        elif S_ISREG(mode):
            # It's a file, call the callback function
            callback(pathname)
        else:
            # Unknown file type, print a message
            print('Skipping %s' % pathname)
def visitfile(file):
    print('visiting', file)
if __name__ == '__main__':
    walktree(sys.argv[1], visitfile)
An additional utility function is provided to convert a fileâs mode in a human readable string:
- stat.filemode(mode)Â¶
- Convert a fileâs mode to a string of the form â-rwxrwxrwxâ. Added in version 3.3.
All the variables below are simply symbolic indexes into the 10-tuple returned
by os.stat(), os.fstat() or os.lstat().
- stat.ST_MODEÂ¶
- Inode protection mode.
- stat.ST_INOÂ¶
- Inode number.
- stat.ST_DEVÂ¶
- Device inode resides on.
- stat.ST_NLINKÂ¶
- Number of links to the inode.
- stat.ST_UIDÂ¶
- User id of the owner.
- stat.ST_GIDÂ¶
- Group id of the owner.
- stat.ST_SIZEÂ¶
- Size in bytes of a plain file; amount of data waiting on some special files.
- stat.ST_ATIMEÂ¶
- Time of last access.
- stat.ST_MTIMEÂ¶
- Time of last modification.
- stat.ST_CTIMEÂ¶
- The âctimeâ as reported by the operating system. On some systems (like Unix) is the time of the last metadata change, and, on others (like Windows), is the creation time (see platform documentation for details).
The interpretation of âfile sizeâ changes according to the file type.  For plain
files this is the size of the file in bytes.  For FIFOs and sockets under most
flavors of Unix (including Linux in particular), the âsizeâ is the number of
bytes waiting to be read at the time of the call to os.stat(),
os.fstat(), or os.lstat(); this can sometimes be useful, especially
for polling one of these special files after a non-blocking open.  The meaning
of the size field for other character and block devices varies more, depending
on the implementation of the underlying system call.
The variables below define the flags used in the ST_MODE field.
Use of the functions above is more portable than use of the first set of flags:
- stat.S_IFSOCKÂ¶
- Socket.
- stat.S_IFLNKÂ¶
- Symbolic link.
- stat.S_IFREGÂ¶
- Regular file.
- stat.S_IFBLKÂ¶
- Block device.
- stat.S_IFDIRÂ¶
- Directory.
- stat.S_IFCHRÂ¶
- Character device.
- stat.S_IFIFOÂ¶
- FIFO.
- stat.S_IFDOORÂ¶
- Door. Added in version 3.4.
- stat.S_IFPORTÂ¶
- Event port. Added in version 3.4.
- stat.S_IFWHTÂ¶
- Whiteout. Added in version 3.4.
Note
S_IFDOOR, S_IFPORT or S_IFWHT are defined as
0 when the platform does not have support for the file types.
The following flags can also be used in the mode argument of os.chmod():
- stat.S_ISUIDÂ¶
- Set UID bit.
- stat.S_ISGIDÂ¶
- Set-group-ID bit. This bit has several special uses. For a directory it indicates that BSD semantics is to be used for that directory: files created there inherit their group ID from the directory, not from the effective group ID of the creating process, and directories created there will also get the S_ISGID bit set.  For a
file that does not have the group execution bit (S_IXGRP )
set, the set-group-ID bit indicates mandatory file/record locking
(see alsoS_ENFMT ).
- stat.S_ISVTXÂ¶
- Sticky bit. When this bit is set on a directory it means that a file in that directory can be renamed or deleted only by the owner of the file, by the owner of the directory, or by a privileged process.
- stat.S_IRWXUÂ¶
- Mask for file owner permissions.
- stat.S_IRUSRÂ¶
- Owner has read permission.
- stat.S_IWUSRÂ¶
- Owner has write permission.
- stat.S_IXUSRÂ¶
- Owner has execute permission.
- stat.S_IRWXGÂ¶
- Mask for group permissions.
- stat.S_IRGRPÂ¶
- Group has read permission.
- stat.S_IWGRPÂ¶
- Group has write permission.
- stat.S_IXGRPÂ¶
- Group has execute permission.
- stat.S_IRWXOÂ¶
- Mask for permissions for others (not in group).
- stat.S_IROTHÂ¶
- Others have read permission.
- stat.S_IWOTHÂ¶
- Others have write permission.
- stat.S_IXOTHÂ¶
- Others have execute permission.
- stat.S_ENFMTÂ¶
- System V file locking enforcement. This flag is shared with S_ISGID :
file/record locking is enforced on files that do not have the group
execution bit (S_IXGRP ) set.
The following flags can be used in the flags argument of os.chflags():
- stat.UF_SETTABLEÂ¶
- All user settable flags. Added in version 3.13.
- stat.UF_NODUMPÂ¶
- Do not dump the file.
- stat.UF_IMMUTABLEÂ¶
- The file may not be changed.
- stat.UF_APPENDÂ¶
- The file may only be appended to.
- stat.UF_OPAQUEÂ¶
- The directory is opaque when viewed through a union stack.
- stat.UF_NOUNLINKÂ¶
- The file may not be renamed or deleted.
- stat.UF_COMPRESSEDÂ¶
- The file is stored compressed (macOS 10.6+).
- stat.UF_TRACKEDÂ¶
- Used for handling document IDs (macOS) Added in version 3.13.
- stat.UF_DATAVAULTÂ¶
- The file needs an entitlement for reading or writing (macOS 10.13+) Added in version 3.13.
- stat.UF_HIDDENÂ¶
- The file should not be displayed in a GUI (macOS 10.5+).
- stat.SF_SETTABLEÂ¶
- All super-user changeable flags Added in version 3.13.
- stat.SF_SUPPORTEDÂ¶
- All super-user supported flags Availability: macOS Added in version 3.13.
- stat.SF_SYNTHETICÂ¶
- All super-user read-only synthetic flags Availability: macOS Added in version 3.13.
- stat.SF_ARCHIVEDÂ¶
- The file may be archived.
- stat.SF_IMMUTABLEÂ¶
- The file may not be changed.
- stat.SF_APPENDÂ¶
- The file may only be appended to.
- stat.SF_RESTRICTEDÂ¶
- The file needs an entitlement to write to (macOS 10.13+) Added in version 3.13.
- stat.SF_NOUNLINKÂ¶
- The file may not be renamed or deleted.
- stat.SF_SNAPSHOTÂ¶
- The file is a snapshot file.
- stat.SF_FIRMLINKÂ¶
- The file is a firmlink (macOS 10.15+) Added in version 3.13.
- stat.SF_DATALESSÂ¶
- The file is a dataless object (macOS 10.15+) Added in version 3.13.
See the *BSD or macOS systems man page chflags(2) for more information.
On Windows, the following file attribute constants are available for use when
testing bits in the st_file_attributes member returned by os.stat().
See the Windows API documentation
for more detail on the meaning of these constants.
- stat.FILE_ATTRIBUTE_ARCHIVEÂ¶
- stat.FILE_ATTRIBUTE_COMPRESSEDÂ¶
- stat.FILE_ATTRIBUTE_DEVICEÂ¶
- stat.FILE_ATTRIBUTE_DIRECTORYÂ¶
- stat.FILE_ATTRIBUTE_ENCRYPTEDÂ¶
- stat.FILE_ATTRIBUTE_HIDDENÂ¶
- stat.FILE_ATTRIBUTE_INTEGRITY_STREAMÂ¶
- stat.FILE_ATTRIBUTE_NORMALÂ¶
- stat.FILE_ATTRIBUTE_NOT_CONTENT_INDEXEDÂ¶
- stat.FILE_ATTRIBUTE_NO_SCRUB_DATAÂ¶
- stat.FILE_ATTRIBUTE_OFFLINEÂ¶
- stat.FILE_ATTRIBUTE_READONLYÂ¶
- stat.FILE_ATTRIBUTE_REPARSE_POINTÂ¶
- stat.FILE_ATTRIBUTE_SPARSE_FILEÂ¶
- stat.FILE_ATTRIBUTE_SYSTEMÂ¶
- stat.FILE_ATTRIBUTE_TEMPORARYÂ¶
- stat.FILE_ATTRIBUTE_VIRTUALÂ¶
- Added in version 3.5.
On Windows, the following constants are available for comparing against the
st_reparse_tag member returned by os.lstat(). These are well-known
constants, but are not an exhaustive list.