wave â Read and write WAV filesÂ¶
Source code: Lib/wave.py
The wave module provides a convenient interface to the Waveform Audio
âWAVEâ (or âWAVâ) file format. Only uncompressed PCM encoded wave files are
supported.
Changed in version 3.12: Support for WAVE_FORMAT_EXTENSIBLE headers was added, provided that the
extended format is KSDATAFORMAT_SUBTYPE_PCM.
The wave module defines the following function and exception:
- wave.open(file, mode=None)Â¶
- If file is a string, open the file by that name, otherwise treat it as a file-like object. mode can be: 
  - 'rb'
  - Read only mode.
  - 'wb'
  - Write only mode.
 Note that it does not allow read/write WAV files. A mode of 'rb' returns aWave_read object, while a mode of'wb' returns aWave_write object.  If mode is omitted and a
file-like object is passed as file,file.mode is used as the default
value for mode.If you pass in a file-like object, the wave object will not close it when its close() method is called; it is the callerâs responsibility to close
the file object.The open() function may be used in awith statement.  When
thewith block completes, theWave_read.close() orWave_write.close() method is called.Changed in version 3.4: Added support for unseekable files.
- exception wave.ErrorÂ¶
- An error raised when something is impossible because it violates the WAV specification or hits an implementation deficiency.
Wave_read ObjectsÂ¶
- class wave.Wave_readÂ¶
- Read a WAV file. Wave_read objects, as returned by open() , have the following methods:
  - close()Â¶
  - Close the stream if it was opened by wave , and make the instance
unusable.  This is called automatically on object collection.
 
  - getnchannels()Â¶
  - Returns number of audio channels ( 1 for mono,2 for stereo).
 
  - getsampwidth()Â¶
  - Returns sample width in bytes.
 
  - getframerate()Â¶
  - Returns sampling frequency.
 
  - getnframes()Â¶
  - Returns number of audio frames.
 
  - getcomptype()Â¶
  - Returns compression type ( 'NONE' is the only supported type).
 
  - getcompname()Â¶
  - Human-readable version of getcomptype() . Usually'not compressed' parallels'NONE' .
 
  - getparams()Â¶
  - Returns a namedtuple()(nchannels, sampwidth,
framerate, nframes, comptype, compname) , equivalent to output of theget*() methods.
 
  - rewind()Â¶
  - Rewind the file pointer to the beginning of the audio stream.
 The following two methods are defined for compatibility with the old aifc module, and donât do anything interesting.
  - getmarkers()Â¶
  - Returns None .Deprecated since version 3.13, will be removed in version 3.15: The method only existed for compatibility with the aifc module
which has been removed in Python 3.13.
 
  - getmark(id)Â¶
  - Raise an error. Deprecated since version 3.13, will be removed in version 3.15: The method only existed for compatibility with the aifc module
which has been removed in Python 3.13.
 The following two methods define a term âpositionâ which is compatible between them, and is otherwise implementation dependent. 
  - setpos(pos)Â¶
  - Set the file pointer to the specified position.
 
  - tell()Â¶
  - Return current file pointer position.
Wave_write ObjectsÂ¶
- class wave.Wave_writeÂ¶
- Write a WAV file. Wave_write objects, as returned by open() .For seekable output streams, the wave header will automatically be updated
to reflect the number of frames actually written.  For unseekable streams, the
nframes value must be accurate when the first frame data is written.  An
accurate nframes value can be achieved either by callingsetnframes() orsetparams() with the number
of frames that will be written beforeclose() is called and
then usingwriteframesraw() to write the frame data, or by
callingwriteframes() with all of the frame data to be
written.  In the latter casewriteframes() will calculate
the number of frames in the data and set nframes accordingly before writing
the frame data.Changed in version 3.4: Added support for unseekable files. Wave_write objects have the following methods: 
  - close()Â¶
  - Make sure nframes is correct, and close the file if it was opened by wave .  This method is called upon object collection.  It will raise
an exception if the output stream is not seekable and nframes does not
match the number of frames actually written.
 
  - setnchannels(n)Â¶
  - Set the number of channels.
 
  - getnchannels()Â¶
  - Return the number of channels.
 
  - setsampwidth(n)Â¶
  - Set the sample width to n bytes.
 
  - getsampwidth()Â¶
  - Return the sample width in bytes.
 
  - setframerate(n)Â¶
  - Set the frame rate to n. Changed in version 3.2: A non-integral input to this method is rounded to the nearest integer.
 
  - getframerate()Â¶
  - Return the frame rate.
 
  - setnframes(n)Â¶
  - Set the number of frames to n. This will be changed later if the number of frames actually written is different (this update attempt will raise an error if the output stream is not seekable).
 
  - getnframes()Â¶
  - Return the number of audio frames written so far.
 
  - setcomptype(type, name)Â¶
  - Set the compression type and description. At the moment, only compression type NONE is supported, meaning no compression.
 
  - getcomptype()Â¶
  - Return the compression type ( 'NONE' ).
 
  - getcompname()Â¶
  - Return the human-readable compression type name.
 
  - setparams(tuple)Â¶
  - The tuple should be (nchannels, sampwidth, framerate, nframes, comptype,
compname) , with values valid for theset*() methods.  Sets all
parameters.
 
  - getparams()Â¶
  - Return a namedtuple()(nchannels, sampwidth, framerate, nframes, comptype, compname) containing the current output parameters.
 
  - tell()Â¶
  - Return current position in the file, with the same disclaimer for the Wave_read.tell() andWave_read.setpos() methods.
 
  - writeframesraw(data)Â¶
  - Write audio frames, without correcting nframes. Changed in version 3.4: Any bytes-like object is now accepted.
 
  - writeframes(data)Â¶
  - Write audio frames and make sure nframes is correct. It will raise an error if the output stream is not seekable and the total number of frames that have been written after data has been written does not match the previously set value for nframes. Changed in version 3.4: Any bytes-like object is now accepted. Note that it is invalid to set any parameters after calling writeframes() orwriteframesraw() , and any attempt to do so will raisewave.Error .